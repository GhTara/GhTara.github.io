import fs from 'node:fs';
import path from 'node:path';
import { marked } from 'marked';

const root = process.cwd();
const out = path.join(root, 'docs');
const linked = (url, label, extra = '') => `<a href="${url}"${extra}>${label}</a>`;
const links = {
  github: 'https://github.com/GhTara',
  scholar: 'https://scholar.google.com/citations?user=p8arCtwAAAAJ&hl=en',
  linkedin: 'https://www.linkedin.com/in/tara-gheshlaghi',
  email: 'mailto:tara.gheshlaghi@kit.edu',
  medium: 'https://medium.com/@LifeExplorer_/my-first-carrot-cake-a-healthier-birthday-cake-experiment-13bc5b2d79e7'
};
const external = ' target="_blank" rel="noopener noreferrer"';
const nav = [ ['Home','/'], ['Work','/work/'], ['Writing','/writing/'], ['Life','/life/'], ['About','/about/'] ];

function shell(title, description, current, content) {
  const navigation = nav.map(([label, href]) => `<a href="${href}"${current === label ? ' aria-current="page"' : ''}>${label}</a>`).join('');
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#ffffff"><title>${title} · Tara Gheshlaghi</title><meta name="description" content="${description}"><link rel="icon" href="/assets/favicon.svg" type="image/svg+xml"><link rel="stylesheet" href="/assets/style.css"></head><body><a class="skip" href="#main" style="position:absolute;left:-9999px;top:8px;background:white;padding:8px;z-index:10" onfocus="this.style.left='8px'" onblur="this.style.left='-9999px'">Skip to content</a><header class="site-header"><div class="wrap header-inner"><a class="brand" href="/">Tara Gheshlaghi</a><nav class="site-nav" aria-label="Main navigation">${navigation}</nav></div></header><main id="main">${content}</main><footer class="site-footer"><div class="wrap footer-inner"><span>© ${new Date().getUTCFullYear()} Tara Gheshlaghi · Karlsruhe, Germany</span><span>${linked(links.email,'Email')} ${linked(links.github,'GitHub',external)} ${linked(links.linkedin,'LinkedIn',external)} ${linked(links.scholar,'Scholar',external)}</span></div></footer></body></html>`;
}
function write(route, html) {
  const target = path.join(out, route, 'index.html');
  fs.mkdirSync(path.dirname(target), {recursive:true});
  fs.writeFileSync(target, html);
}
function project(num, title, text, meta, href, label='Read more') {
  return `<article class="project"><span class="project-num">${num}</span><div><h3>${title}</h3><p>${text}</p><div class="project-links">${linked(href,label)}</div></div><span class="project-meta">${meta}</span></article>`;
}
function sectionHead(title, copy='') { return `<div class="section-intro"><h2>${title}</h2>${copy ? `<p>${copy}</p>` : ''}</div>`; }

fs.rmSync(out,{recursive:true,force:true});
fs.mkdirSync(path.join(out,'assets'),{recursive:true});
for(const file of ['style.css','favicon.svg','photo.jpg']) fs.copyFileSync(path.join(root,'assets',file),path.join(out,'assets',file));
fs.writeFileSync(path.join(out,'.nojekyll'),'');

const home = `<div class="wrap"><section class="hero"><div><div class="eyebrow">Machine learning · optimization · emerging computing</div><h1>Hi, I’m Tara.</h1><p class="lead">I build machine learning methods for systems that have to work under real physical constraints.</p><p class="sublead">I’m a PhD researcher at Karlsruhe Institute of Technology. My work spans reliable AI, printed and flexible electronics, fault diagnosis, and optimization.</p><div class="link-row">${linked('/work/','Explore my work')}${linked('/about/','More about me')}${linked(links.email,'Get in touch')}</div></div><figure class="portrait" style="margin:0"><img src="/assets/photo.jpg" alt="Tara Gheshlaghi outdoors" width="1152" height="1536"></figure></section><section class="section">${sectionHead('Selected work','From algorithms and experiments to their behavior on imperfect hardware.')}<div class="project-list">${project('01','Reliable printed AI','Generating compact tests and diagnosing faults in printed neuromorphic circuits.','Testing & diagnosis','/work/#reliability')}${project('02','Flexible analog learning','Studying how device variation changes analog classifiers, and how hardware and training can respond.','Hardware-aware ML','/work/#flexible')}${project('03','Medical image analysis','Transformer and convolutional models for 3D radiotherapy dose prediction and organ segmentation.','Medical AI','/work/#medical')}</div></section><section class="section"><div class="split"><div>${sectionHead('Writing')}<div class="note-card"><div class="small-label">Science & ideas</div><h3>Research, explained clearly</h3><p>Notes on machine learning, optimization, reliability, and ideas I’m learning.</p>${linked('/writing/','Explore writing',' class="arrow-link"')}</div></div><div>${sectionHead('Beyond research')}<div class="note-card"><div class="small-label">Life & notes</div><h3>Small experiments, everyday life</h3><p>My first entry is a birthday carrot cake and what I learned from making it.</p>${linked('/life/','Explore life',' class="arrow-link"')}</div></div></div></section></div>`;
write('',shell('Home','Tara Gheshlaghi is a PhD researcher at KIT working on machine learning, optimization, and reliable emerging computing systems.','Home',home));

const work = `<div class="wrap"><div class="page-head"><div class="eyebrow">Research & projects</div><h1>Work</h1><p>I develop and evaluate learning and optimization methods where data, hardware, and reliability meet.</p></div><section class="work-detail" id="reliability"><div class="label">01 / Reliability</div><div><h2>Finding faults with fewer tests</h2><p>Printed neuromorphic circuits and DNN accelerators can fail in ways that ordinary software tests miss. I work on test generation and diagnosis using black-box observations, evolutionary search, and fault-aware evaluation.</p><p>My 2026 VTS work explores compact functional tests across DNN architectures and fault models. My printed-circuit diagnosis research studies how to distinguish faulty components from their outputs.</p><div class="project-links">${linked('https://github.com/GhTara/ATPG-EA','DNN test-generation code',external)} ${linked(links.scholar,'Publications',external)}</div></div></section><section class="work-detail" id="flexible"><div class="label">02 / Emerging hardware</div><div><h2>Learning on imperfect hardware</h2><p>Printed and flexible electronics offer low-cost, resource-efficient inference, but their devices vary. I build hardware-aware learning methods and study how variation, sensor noise, faults, and power constraints affect model behavior.</p><p>Recent work includes robustness-aware training for printed neuromorphic circuits and an analog radial basis function classifier for flexible near-sensor inference.</p><div class="project-links">${linked('https://github.com/KIT-Neuromorphic-Computing/ADAPT-pNC','ADAPT-pNC code',external)}</div></div></section><section class="work-detail" id="physical"><div class="label">03 / Optimization</div><div><h2>From search to physical design</h2><p>I use optimization to explore hard engineering choices, including test generation and device-level placement and routing for printed circuits. The current layout work connects Python-based search with circuit design tools and evaluates candidates against connectivity, geometry, and area constraints.</p></div></section><section class="work-detail" id="medical"><div class="label">04 / Medical AI</div><div><h2>Predicting 3D radiotherapy dose</h2><p>During my master’s research, I worked on CT preprocessing, organ-at-risk segmentation, and 3D dose prediction for head-and-neck radiotherapy. Our cascade model combined convolutional and transformer components.</p><div class="project-links">${linked('https://github.com/GhTara/Dose_Prediction','Code',external)} ${linked('https://doi.org/10.1088/1361-6560/ad209a','Paper',external)}</div></div></section><section class="section"><h2>Selected publications</h2><ul class="publications"><li><strong>Compact Functional Test Pattern Generation for DNNs Using Evolution Strategies.</strong> <span>IEEE VTS, 2026.</span></li><li><strong>Diagnostic Test Generation for Fault Localization in Printed Neuromorphic Circuits.</strong> <span>DATE, 2026.</span></li><li><strong>Power-Constrained Printed Neuromorphic Hardware Training.</strong> <span>DAC, 2025.</span></li><li><strong>ADAPT-pNC: Mitigating Device Variability and Sensor Noise.</strong> <span>DATE, 2025.</span></li><li><strong>A Cascade Transformer-Based Model for 3D Dose Distribution Prediction in Head and Neck Cancer Radiotherapy.</strong> <span>Physics in Medicine & Biology, 2024.</span></li></ul><p style="margin-top:25px">${linked(links.scholar,'Full publication list',external)}</p></section></div>`;
write('work',shell('Work','Research and projects in hardware-aware machine learning, fault diagnosis, optimization, and medical imaging.','Work',work));

const about = `<div class="wrap"><div class="page-head"><div class="eyebrow">A little about me</div><h1>About</h1><p>My path into AI began with medical images. Today I’m interested in what happens when intelligent systems leave ideal software environments.</p></div><section class="section" style="padding-top:0;border:0"><div class="text-block"><p>I studied Computer Engineering and then Artificial Intelligence at Shahid Beheshti University in Tehran. For my master’s research, I worked with 3D medical images and developed models for radiotherapy planning. That experience taught me to care about the entire path from raw data to a useful result.</p><p>In November 2023, I joined the Chair of Dependable Nano Computing at Karlsruhe Institute of Technology as a PhD researcher. I now work on machine learning and optimization for emerging computing systems: how to train models for variable devices, detect faults, generate useful tests, and make resource-constrained systems more dependable.</p><p>I enjoy work that crosses disciplines and becomes something concrete. Alongside my research, I supervise student research and software projects, and I like explaining technical ideas so people from different backgrounds can work together.</p></div></section><div class="band"><p>Good models matter. So do the data, devices, constraints, and people around them.</p></div><section class="section"><div class="split"><div><h2>Background</h2><p><strong>PhD researcher</strong><br>Karlsruhe Institute of Technology · 2023–present</p><p><strong>MSc, Artificial Intelligence</strong><br>Shahid Beheshti University · 2023</p><p><strong>BSc, Computer Engineering</strong><br>Shahid Beheshti University · 2020</p></div><div><h2>Connect</h2><p class="contact-line">The easiest way to reach me is ${linked(links.email,'by email')}.</p><p>${linked(links.github,'GitHub',external)} · ${linked(links.linkedin,'LinkedIn',external)} · ${linked(links.scholar,'Google Scholar',external)}</p><p class="quiet">Based in Karlsruhe, Germany.</p></div></div></section></div>`;
write('about',shell('About','The path from medical image analysis to reliable, hardware-aware AI.','About',about));

function parsePost(file, category) {
  const raw=fs.readFileSync(file,'utf8');
  const match=raw.match(/^---\n([\s\S]*?)\n---\n([\s\S]*)$/);
  if(!match) throw new Error(`Missing front matter: ${file}`);
  const meta=Object.fromEntries(match[1].split('\n').filter(Boolean).map(line=>{const n=line.indexOf(':');return [line.slice(0,n).trim(),line.slice(n+1).trim()]}));
  for(const key of ['title','date','description']) if(!meta[key]) throw new Error(`Missing ${key}: ${file}`);
  const slug=path.basename(file,'.md');
  const route=`${category}/${slug}`;
  const article=`<div class="wrap article"><div class="small-label">${category==='science'?'Science & ideas':'Life & notes'} · ${meta.date}</div><h1>${meta.title}</h1><p class="lead quiet">${meta.description}</p><div class="article-body">${marked.parse(match[2])}</div><p style="margin-top:60px">${linked(category==='science'?'/writing/':'/life/','← Back to '+(category==='science'?'writing':'life'))}</p></div>`;
  const math=category==='science' ? '<script>window.MathJax={tex:{inlineMath:[["$","$"],["\\\\(","\\\\)"]]}};</script><script defer src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-chtml.js"></script>' : '';
  write(route,shell(meta.title,meta.description,category==='science'?'Writing':'Life',article).replace('</body>',`${math}</body>`));
  return {...meta,route:`/${route}/`};
}
function readPosts(category) {
  const dir=path.join(root,'content',category);
  if(!fs.existsSync(dir)) return [];
  return fs.readdirSync(dir).filter(x=>x.endsWith('.md') && !x.startsWith('_')).map(x=>parsePost(path.join(dir,x),category)).sort((a,b)=>b.date.localeCompare(a.date));
}
const science=readPosts('science');
const life=readPosts('life');
const postCard=p=>`<article class="entry"><div class="small-label">${p.date}</div><h2>${linked(p.route,p.title)}</h2><p>${p.description}</p></article>`;
const scienceList=science.length?science.map(postCard).join(''):`<div class="note-card"><h3>Notes are on their way.</h3><p>Research explanations and concepts I’m learning will live here.</p></div>`;
const writing=`<div class="wrap"><div class="page-head"><div class="eyebrow">Science & ideas</div><h1>Writing</h1><p>Technical notes on machine learning, optimization, reliable systems, and the questions behind my research.</p></div><section class="section" style="padding-top:0;border:0">${scienceList}</section><section class="section"><h2>Another side of the notebook</h2><p class="quiet">Stories about life, cooking, and things I try live in a separate place.</p>${linked('/life/','Visit Life',' class="arrow-link"')}</section></div>`;
write('writing',shell('Writing','Scientific writing by Tara Gheshlaghi on machine learning, optimization, and reliable computing.','Writing',writing));
const lifePage=`<div class="wrap"><div class="page-head"><div class="eyebrow">Beyond research</div><h1>Life</h1><p>Cooking, exploring, learning, and the small experiments that make up a life.</p></div><section class="section" style="padding-top:0;border:0"><article class="entry"><div class="small-label">Life & notes · June 2026 · on Medium</div><h2>${linked(links.medium,'My First Carrot Cake: A Healthier Birthday Cake Experiment',external)}</h2><p>A birthday cake experiment and a reminder that trying something new doesn’t require getting it perfect.</p>${linked(links.medium,'Read on Medium',external+' class="arrow-link"')}</article>${life.map(postCard).join('')}</section><section class="section"><div class="text-block"><h2>A growing journal</h2><p>I’ll add more of my journey here over time. For now, this first story feels like a good place to begin.</p></div></section></div>`;
write('life',shell('Life','A personal journal of cooking, learning, and everyday experiments by Tara Gheshlaghi.','Life',lifePage));
fs.writeFileSync(path.join(out,'robots.txt'),'User-agent: *\nAllow: /\n');
console.log(`Built ${science.length} science and ${life.length} life posts in docs/`);
